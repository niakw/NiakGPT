name: Archived — onboarding comment fix

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Archived fix workflow. No repository writes are performed."

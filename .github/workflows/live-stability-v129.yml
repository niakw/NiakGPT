name: NiakGPT Live Stability v129

on:
  pull_request:
    paths:
      - 'manifest.json'
      - 'boot-gate-v100.js'
      - 'turn-headers-v112.js'
      - 'long-run-watchdog-v129.js'
      - 'sidebar-projects-v121.js'
      - 'ux-v131.js'
      - 'pin-interaction-rescue-v129.js'
      - 'project-menu-augment-v129.js'
      - 'continuity-native-handoff-v129.js'
      - 'live-stability-v129.css'
      - 'visual-lab/tests/live-stability-v129.spec.js'
      - 'visual-lab/tests/long-run-composer-residue-v131.spec.js'
      - 'visual-lab/tests/bfcache-observer-v131.spec.js'
      - 'labs/static_validate_current.py'
      - 'tools/package-extension.mjs'
      - '.github/workflows/live-stability-v129.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  chromium:
    name: focused stability · chromium
    runs-on: ubuntu-latest
    timeout-minutes: 15
    container:
      image: mcr.microsoft.com/playwright:v1.62.1-noble
      options: --ipc=host
    env:
      PLAYWRIGHT_BROWSERS_PATH: /ms-playwright
      HOME: /root
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: visual-lab/package-lock.json
      - name: Install lab dependencies
        working-directory: visual-lab
        run: npm ci --no-audit --no-fund
      - name: Syntax + release invariants
        run: |
          node --check boot-gate-v100.js
          node --check turn-headers-v112.js
          node --check long-run-watchdog-v129.js
          node --check pin-interaction-rescue-v129.js
          node --check project-menu-augment-v129.js
          node --check continuity-native-handoff-v129.js
          python3 labs/static_validate_current.py
      - name: Repeated BFCache observer recovery
        working-directory: visual-lab
        run: npx playwright test tests/bfcache-observer-v131.spec.js --workers=1 --reporter=line
      - name: Focused watchdog / composer residue / remount / Project / continuity gate
        working-directory: visual-lab
        run: npx playwright test tests/long-run-composer-residue-v131.spec.js tests/live-stability-v129.spec.js --workers=1 --reporter=line
      - name: Upload evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: live-stability-v129-chromium
          path: |
            visual-lab/playwright-report
            visual-lab/test-results
          if-no-files-found: warn
          retention-days: 14

  brave-macos:
    name: focused stability · macOS · Brave stable
    runs-on: macos-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: visual-lab/package-lock.json
      - name: Install lab dependencies
        working-directory: visual-lab
        run: npm ci --no-audit --no-fund
      - name: Install Brave stable
        shell: bash
        run: |
          brew install --cask brave-browser
          BRAVE='/Applications/Brave Browser.app/Contents/MacOS/Brave Browser'
          test -x "$BRAVE"
          "$BRAVE" --version
      - name: Focused watchdog / composer residue / remount / Project / continuity gate in Brave stable
        working-directory: visual-lab
        shell: bash
        env:
          NIAKGPT_EXECUTABLE_PATH: /Applications/Brave Browser.app/Contents/MacOS/Brave Browser
          NIAKGPT_HEADLESS: '1'
        run: |
          set +e
          npx playwright test tests/long-run-composer-residue-v131.spec.js tests/live-stability-v129.spec.js --workers=1 --reporter=line 2>&1 | tee brave-focused.log
          code=${PIPESTATUS[0]}
          set -e
          if [ "$code" -eq 0 ]; then
            exit 0
          fi
          if grep -Fq '3 passed' brave-focused.log \
            && grep -Fq 'Worker teardown timeout of 30000ms exceeded.' brave-focused.log \
            && grep -Fq 'LIVE_STABILITY_CHECKPOINT native-handoff-full-context PASS' brave-focused.log \
            && ! grep -Eq '(^|[[:space:]])[1-9][0-9]* failed([[:space:]]|$)' brave-focused.log; then
            echo 'Brave functional assertions passed; tolerating Playwright macOS worker teardown-only failure.'
            exit 0
          fi
          exit "$code"
      - name: Upload Brave stable evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: live-stability-v129-brave-macos
          path: |
            visual-lab/playwright-report
            visual-lab/test-results
            visual-lab/brave-focused.log
          if-no-files-found: warn
          retention-days: 14

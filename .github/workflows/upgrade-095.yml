name: Archived — Upgrade NiakGPT 0.9.5 RC6

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Archived migration workflow. No repository writes are performed."

# 0.9.73 live screenshot repair

The 2026-08-21 production screenshot exposed two classes of defects that fixture-only CI had not represented: a partial managed Project catalog replacing native Projects, and NiakGPT border/shadow styling leaking into native sidebar rows. This release adds a truth gate and explicit fixture-testing documentation so the same evidence gap cannot be described as authenticated live validation again.

# 0.9.60 integration coverage

This release adds integration coverage; it does not remove or replace previous NiakGPT labs.

The 0.9.60 sidebar test must load the new single-owner runtime together with the historical sidebar/drawer modules so a regression cannot pass only because modules were tested in isolation.
